---
- name: Installeer K3s 
  shell: curl -sfL https://get.k3s.io | sh -s - --disable traefik --disable servicelb
  args:
    creates: /usr/local/bin/k3s

- name: Wacht op kubeconfig en maak leesbaar
  file:
    path: /etc/rancher/k3s/k3s.yaml
    mode: '0644'